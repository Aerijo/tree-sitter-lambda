{
  "name": "tree-sitter-lambda",
  "version": "0.0.0",
  "description": "Custom lambda expression grammar for tree-sitter",
  "main": "index.js",
  "repository": "https://github.com/Aerijo/tree-sitter-lambda",
  "bugs": "https://github.com/Aerijo/tree-sitter-lambda/issues",
  "keywords": [
    "lambda",
    "grammar",
    "syntax",
    "highlighting",
    "parser",
    "lexer"
  ],
  "author": "Benjamin Gray",
  "license": "MIT",
  "dependencies": {
    "nan": "^2.4.0",
    "tree-sitter-cli": "0.9.x",
    "tree-sitter": "0.9.x"
  },
  "devDependencies": {
    "tree-sitter-cli": "0.9.x"
  },
  "scripts": {
    "build": "tree-sitter generate && node-gyp rebuild",
    "test": "tree-sitter test && tree-sitter parse examples --quiet --time"
  }
}
